info:
  name: keyrotation
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8080/admin/keys/rotate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
