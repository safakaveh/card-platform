info:
  name: all
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/users?page=0&limit=10
  params:
    - name: page
      value: "0"
      type: query
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
