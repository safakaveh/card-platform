info:
  name: jwks.json
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/.well-known/jwks.json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
